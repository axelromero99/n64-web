node_modules/
dist/
*.local

# Cloudflare Wrangler (estado local del deploy)
.wrangler/
.dev.vars

# Tooling local (agentes / worktrees), no es parte del proyecto
.claude/

# ROMs: nunca versionar (legal). El usuario las carga desde su disco.
public/roms/*
!public/roms/PONER-ROMS-AQUI.txt
*.z64
*.n64
*.v64
